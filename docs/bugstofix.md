Resolved entries split out to
[done/bugstofix-resolved-2026-08.md](done/bugstofix-resolved-2026-08.md), 2026-08-15. This file
keeps only what's still open.

```json
[
  {
    "file": "src/OpenTail.Stingray.Cpu/DeepSeekMoeGraph.cs",
    "line": 36,
    "summary": "STILL UNFIXED, but no longer on the exercised code path. This standalone file's column-major router bug was never touched this session -- the new CPU MLA work (ForwardPass.cs) reuses the codebase's existing, generic MoE routing (MoeFfn/MoeFfnBatched, already used correctly by Qwen3-MoE/OLMoE/qwen35moe) rather than this file's helpers, gated per-layer on the new IsMoeLayer(layer) so deepseek2's leading_dense_block_count is respected. Whether that generic path is itself fully correct for deepseek2's routing wasn't specifically re-verified as part of the numerical-bug hunt below.",
    "failure_scenario": "Unchanged from before: any caller that DOES route through this file's SelectTopKExperts would still get random expert selection. Not currently reachable from CPU deepseek2 inference, but the file itself remains broken and undeleted -- worth removing or fixing if anything ever calls it."
  },
  {
    "file": "src/OpenTail.Stingray.Cpu/MlaAttention.cs",
    "line": 42,
    "summary": "STILL UNFIXED, but no longer on the exercised code path. The new CPU MLA work (ForwardPass.cs: MlaComputeQkv/MlaComputeQkvBatched) is freshly written, quantization-aware code that does NOT call this file's CompressKvLatent/UncompressKv helpers at all -- their column-major transpose bug was never exercised or fixed.",
    "failure_scenario": "Unchanged from before for any caller of this standalone helper class. Not currently reachable from CPU deepseek2 inference (see ModelCompatibility.cs:460 entry below for what IS reachable, and its own unresolved numerical bug) -- worth removing or fixing if anything ever calls it."
  },
  {
    "file": "src/OpenTail.Stingray.Cpu/IqCodebooks.cs",
    "line": 33,
    "summary": "IQ2_XXS/IQ3_XXS/IQ3_S/IQ4_XS grid tables are procedurally fabricated from a bit-pattern formula rather than the real calibrated GGML codebook constants, and the matching decoders skip the real format's 9-bit index/7-bit sign-mask/per-group scale.",
    "failure_scenario": "Any GGUF quantized with these IQ formats dequantizes to plausible-looking but unrelated weight values, silently degrading generation to incoherence; the existing test only checks shape/NaN-absence, not reference values."
  },
  {
    "file": "src/OpenTail.Stingray.Engine/ModelCompatibility.cs",
    "line": 460,
    "summary": "PARTIALLY ADDRESSED, CPU only, STILL BROKEN. ForwardPass.cs (CPU dense path, the one RunCommand constructs when no GPU offload is requested) now loads the MLA-specific attn_kv_a_mqa/attn_kv_a_norm/attn_kv_b tensors, implements compress->RMSNorm->decompress K/V (legacy unsplit wkv_b layout only, matching DeepSeek-V2-Lite's actual GGUF), a YaRN-scaled RoPE table (SimdKernels.BuildYarnRopeTable), the deepseek2-specific kq_scale/mscale attention-score correction (ModelGraph.cs), and per-layer dense/MoE dispatch for leading_dense_block_count. Verified end-to-end against models/DeepSeek-V2-Lite-Chat.Q2_K.gguf: the model loads, prefills, and decodes with zero crashes across 27 layers -- a first for this architecture in the codebase. THREE real bugs were found and fixed in the YaRN/kq_scale formula chain by tracing the exact derivation through examples/llama.cpp/llama.cpp's src/models/deepseek2.cpp and src/llama-context.cpp (the raw rope.scaling.yarn_log_multiplier GGUF value needs the same /0.1f 'cancel the convert script' correction llama.cpp applies at load time [TAG_DEEPSEEK2_YARN_LOG_MUL_FIX]; the YaRN RoPE table's attn_factor and the kq_scale formula's attn_factor_org were both computed from a wrong constant instead of correctly reproducing llama-context.cpp's deliberate pre-divide-then-cancel derivation of cparams.yarn_attn_factor). Despite all three fixes (each independently verified formula-by-formula against the reference source), generated output is still numerically wrong for trivial prompts (e.g. 'The capital of France is' does not produce 'Paris'), while the same GGUF via a prebuilt llama.cpp binary (tools/llama.cpp/llama-cli.exe, b10306, no compiler needed -- see scripts/setup-llamacpp.ps1) correctly generates coherent, correct text. Extensively checked and ruled out as the remaining cause: Q/K/V tensor layout offsets against deepseek2.cpp's exact view offsets, the partial-RoPE pairing convention, MHA-not-MQA at the cache level (numKvHeads), no double-application of the attention scale, the Flash-64 fast-attention path correctly excluding headDim=192, and the Q2_K dequantization/matmul kernels at every batched-dispatch tier (verified byte-for-byte against ggml's block_q2_K struct and dequantize_row_q2_K). Root cause NOT identified. GPU backends (GpuForwardPass/CudaForwardPass/hybrid variants) remain entirely untouched -- CPU-only was the explicitly agreed scope, and was deliberately not extended given CPU itself isn't correct yet.",
    "failure_scenario": "No longer throws on load (the original Missing-tensor crash this entry described is resolved). Current failure: silently wrong generation with no crash, no NaN, no exception -- structurally valid tokens, numerically incorrect content ('The capital of France is duringermeconpolivari twin,alam' at temp 0, as of the 2026-08-21 resumption below). Next step if resumed: get real ground-truth intermediate tensor values (e.g. building llama.cpp's eval-callback tool, which needs the MSVC C++ build tools this session deliberately avoided installing in favor of the faster prebuilt-binary path) to diff against this codebase's actual intermediate values, since formula-level re-derivation against the reference source has been exhausted twice now without finding the remaining bug."
  },
  {
    "file": "src/OpenTail.Stingray.Engine/ModelCompatibility.cs",
    "line": 461,
    "summary": "2026-08-21 resumption of the entry above via systematic process-of-elimination: mapped every MLA/YaRN/kq_scale/RoPE-type-dispatch code segment (ModelGraph.cs's mscale/kq_scale/attn_factor_org derivation, ModelGraph.cs's isNeoxRope arch list, ForwardPass.Decode.cs's MlaComputeQkv/MlaComputeQkvBatched) against examples/llama.cpp/llama.cpp's src/models/deepseek2.cpp line-by-line. Confirmed correct: the kq_scale/mscale formula matches the reference's attn_factor_org/mscale/kq_scale derivation exactly (already fixed in a prior session, per the entry above); deepseek2's RoPE type is genuinely LLAMA_ROPE_TYPE_NORM/interleaved-pairs, not NEOX (confirmed via llama-model.cpp's rope-type switch grouping LLM_ARCH_DEEPSEEK2 with LLM_ARCH_LLAMA), matching this engine's isNeoxRope=false default (deepseek2 was never added to that arch list, which is correct, not an oversight); the Q/K/V reorder and wkv_a_mqa->attn_kv_a_norm->wkv_b decompress sequence in MlaComputeQkv matches the reference's non-absorbed ('MLA without absorption optimization') MHA branch tensor-for-tensor. One genuine, previously-unexamined gap WAS found and fixed: {arch}.expert_weights_norm/expert_weights_scale (llama.cpp's LLM_KV_EXPERT_WEIGHTS_NORM/SCALE, applied post-top-k-selection in build_moe_ffn) were never read from GGUF metadata anywhere in this codebase -- NormalizeMoeTopKWeights was a hardcoded olmoe-only-false heuristic, not the GGUF's real value, and expert_weights_scale (routed_scaling_factor) wasn't applied at all. Added ModelHyperparams.ExpertWeightsScale, wired {arch}.expert_weights_norm into NormalizeMoeTopKWeights's GGUF read (ModelGraph.cs), and applied ExpertWeightsScale to each selected expert's weight in both MoeFfn and MoeFfnBatched (ForwardPass.Moe.cs) -- a real correctness fix worth keeping for other DeepSeek-V2/V3/GLM-MoE checkpoints that DO declare non-default values. Re-tested end-to-end against a freshly re-downloaded models/DeepSeek-V2-Lite-Chat.Q2_K.gguf (mradermacher/DeepSeek-V2-Lite-Chat-GGUF): NO CHANGE in output. This specific GGUF has no expert_weights_norm key at all (falls back to the same true-by-default behavior as before the fix) and declares expert_weights_scale=1 (a no-op), so this gap was not the cause for THIS checkpoint specifically, even though it's a real latent bug for others. Also confirmed via list-metadata that rope.scaling.type=yarn IS active for this GGUF (factor=40, original_context_length=4096, yarn_log_multiplier=0.0707) -- ruling out an initial assumption this session that the Lite variant skips YaRN scaling entirely; the mscale/kq_scale/YaRN-table formula chain is genuinely exercised on every token and remains a live suspect despite two independent rounds of formula-level re-derivation finding no further error there.",
    "failure_scenario": "Same as the entry above -- deepseek2 remains un-admitted in ModelCompatibility.cs's allowlist. The expert_weights_norm/scale fix is real and should stay in the codebase (it's now correctly read for any future MoE architecture whose GGUF declares those keys), but does not by itself unblock deepseek2. Next step if resumed: ground-truth per-layer intermediate diffing (see the entry above), NOT another pass of formula re-reading -- two rounds of that (this session and the one before it) have each found real-but-non-root-cause issues rather than the actual defect, suggesting the remaining bug is either in a mechanism not yet mapped (Q2_K dequant was checked before, but not re-checked this round) or requires numeric ground truth to localize rather than further code reading."
  },
  {
    "file": "src/OpenTail.Stingray.Engine/ModelCompatibility.cs",
    "line": 461,
    "summary": "2026-08-21, SAME DAY continuation: built real ground-truth intermediate tensors via examples/llama.cpp/llama.cpp's examples/eval-callback (llama.cpp has no C++ toolchain installed by default, but VS Build Tools 18 + CMake ARE present on this machine at 'C:/Program Files (x86)/Microsoft Visual Studio/18/BuildTools' -- built llama-eval-callback.exe via CMake+Ninja+MSVC against the vendored source tree, ~1 min build). CRITICAL METHODOLOGY BUG FOUND FIRST: every prior comparison across BOTH this session and the previous one was silently comparing this engine's CHAT-TEMPLATE-WRAPPED prompt ('<|begin_of_sentence|>User: ...\\n\\nAssistant:', 12 tokens) against llama-cli's RAW completion of the literal prompt string (6 tokens) -- completely different token sequences, invalidating every 'wrong output' comparison made so far as apples-to-oranges. Fixed for testing via the engine's existing (undocumented in --help) STINGRAY_RAW_PROMPT=1 env var, which reproduces llama-cli's raw-completion tokenization exactly (verified token-for-token identical: 100000,549,6077,280,7239,317). Added temporary env-gated tracing (STINGRAY_MLA_TRACE=1, ForwardPass.cs/ForwardPass.Decode.cs/ForwardPass.PrefillCore.cs, kept in the codebase for the next session) and diffed layer-0 intermediate sums against llama-eval-callback's real per-tensor dumps for the SAME 6-token prompt: attn_norm (RMSNorm + F32 attn_norm.weight, BEFORE any quantized matmul) matches to 5 significant figures (49.207386 vs reference 49.207390) -- the embedding lookup, RMSNorm formula, and eps are all confirmed correct. Divergence begins at the very FIRST Q2_K-quantized matmul (blk.0.attn_kv_a_mqa.weight, [2048,576]): our kv_cmpr_pe individual per-element values (token 0, first/last 3 of 576) are off by roughly 1-12% from the reference eval-callback's dump (e.g. -0.2741 ref vs -0.2420 ours), while the SAME-shaped, SAME-dtype blk.0.attn_q.weight matmul ([2048,3072], also Q2_K) shows a smaller ~1.3% aggregate divergence -- i.e. two Q2_K matmuls over the same 2048-wide input diverge from ground truth by visibly DIFFERENT amounts, which is inconsistent with either (a) both engines correctly decoding the same deterministic Q2_K bytes (that would match exactly, not diverge at all) or (b) uniform FP32 rounding noise (which wouldn't vary 1.3% vs up to 12% between two structurally identical kernel calls). Read DotQ2K/DotQ2K_Scalar (SimdKernels.cs:3277) end-to-end against ggml's real dequantize_row_q2_K block/shift/scale math -- structurally identical, no bug found by inspection alone this pass. NOTEWORTHY CONNECTION: this engine already has a SEPARATE, unresolved open bug (this file, the ForwardPass.cs:1559 entry below) describing PrefillCore/PrefillCoreAttention producing measurably different logits for batched multi-token prefill vs sequential token-by-token decode specifically at prompt lengths NOT aligned to the batched matmul kernel's internal tile/dispatch grouping (their reproduction: length 33 fails, lengths 2 and 8 pass). This session's deepseek2 test is exactly this shape of case: N=6 tokens (not tile-aligned) through MatMulBatchedCached, with the two diverging matmuls differing specifically in OUTPUT row count (3072 vs 576) -- a very plausible SHARED root cause between the two entries, not yet confirmed.",
    "failure_scenario": "Two concrete, narrower next steps for whoever resumes: (1) re-run this session's exact layer-0 trace (STINGRAY_RAW_PROMPT=1 STINGRAY_MLA_TRACE=1, prompt 'The capital of France is', -g 0 -c 32) with F32 or Q8_0 requantized weights instead of Q2_K -- if the divergence disappears or shrinks to noise-floor, that confirms a Q2_K-specific (not MLA-specific) kernel bug and directly implicates DotQ2K/MatMulBatchedCached, tying this entry to the ForwardPass.cs:1559 tiling-divergence entry. (2) if it persists even at F32, the bug is elsewhere in MlaComputeQkvBatched/MlaComputeQkv despite this session's structural reference-matching (see the entry above) -- next candidate would be per-token vs batched RESULT divergence specifically (compare this session's PrefillCore-batched trace sums against the sequential MlaComputeQkv single-token path for the same position, which was NOT done this session due to time constraints, only structural code-reading was done for the sequential path)."
  },
  {
    "file": "src/OpenTail.Stingray.Engine/ModelCompatibility.cs",
    "line": 461,
    "summary": "2026-08-21, SAME DAY, third pass -- ran BOTH suggested next steps from the entry above, plus found and fixed a SECOND real bug. (1) Q8_0 isolation test: used the vendored llama.cpp source tree's already-built MSVC+CMake toolchain (this session's earlier eval-callback build) to build llama-quantize.exe, then force-requantized DeepSeek-V2-Lite-Chat.Q2_K.gguf to Q8_0 (--allow-requantize; the resulting weights are Q8_0-lossless copies of the ALREADY Q2_K-lossy values, not the original F16 weights, but sufficient to isolate this engine's Q2_K kernel specifically). Re-ran the identical layer-0 trace against the Q8_0 file and a fresh llama-eval-callback dump of it: divergence from ground truth shrank substantially at every checkpoint (e.g. kv_cmpr_pe(full576) 2.3%->0.44%, kv_cmpr(post-norm) 5.9%->1.1%, l_out 4.1%->0.3%) while attn_norm matched to 6 significant figures in BOTH quant levels -- CONFIRMS the layer-0 divergence is Q2_K-dequant/kernel-precision-specific, NOT a PrefillCore/PrefillCoreAttention batched-tiling bug (ruling out this entry's tie to the ForwardPass.cs:1559 entry, at least for deepseek2/this shape of test -- that entry's OWN bug, at prompt length 33, remains open and unrelated). Confirmed via a real reference llama-cli run that Q8_0-from-Q2_K weights are NOT the bottleneck for correctness -- llama.cpp still produces 'Paris is the capital of France.' from them, so quantization precision alone cannot explain wrong FINAL output, only the small per-layer numeric deltas measured. (2) MoE ground truth: llama-eval-callback's graph dump for layer 1 (the first MoE layer, deepseek2's leading_dense_block_count=1 means layer 0 is dense) revealed NO renormalization node at all between the top-k softmax probs (ffn_moe_probs) and the weighted expert-output combine (ffn_moe_weighted) -- i.e. ground truth for THIS checkpoint uses expert_weights_norm=false. Cross-checked against every MoE architecture's own C++ loader in examples/llama.cpp/llama.cpp/src/models/: deepseek2.cpp (and bailingmoe/glm4-moe/cohere2moe/dots1/minimax-m3/nemotron-h/step35/afmoe/deepseek32/deepseek2ocr) all call ml.get_key(LLM_KV_EXPERT_WEIGHTS_NORM, hparams.expert_weights_norm, false) -- an explicit FALSE default, matching llama-hparams.h's own field default (line 101) -- while qwen3moe.cpp and olmoe.cpp are DIFFERENT: they pass a LITERAL true/false directly to build_moe_ffn in C++ and never read any GGUF key at all. This session's EARLIER fix (the entry above) got the fallback backwards: it defaulted to TRUE for every architecture except olmoe when the GGUF key was absent, which is exactly backwards for deepseek2 (and every other GGUF-driven MoE arch) while coincidentally still being correct for qwen3moe/olmoe (whose hardcoded C++ values happen to match !arch.Equals('olmoe')). Fixed in ModelGraph.cs: qwen3moe and olmoe now get their literal hardcoded values directly (not read from GGUF at all, matching upstream exactly); every other architecture reads {arch}.expert_weights_norm with a FALSE fallback, matching llama.cpp's real default. Re-tested: layer-1 top-6 expert SELECTION now matches ground truth almost exactly -- 5 of 6 selected expert indices identical (57, 60, 8, 26, 43) with weights agreeing to ~0.5%, the 6th being a genuine near-tie (ref picks expert 56 at weight 0.0352, ours picks expert 11 at weight 0.0352 -- same weight to 4 decimals, i.e. this is boundary noise from the small upstream numeric deltas already characterized in (1), not a separate bug). This is a REAL, decisive confirmation the norm-default fix was correct.",
    "failure_scenario": "Despite BOTH real, ground-truth-confirmed fixes (Q2_K precision now understood and bounded via the Q8_0 isolation test showing PrefillCore's tiling is NOT at fault; expert_weights_norm default corrected and expert-selection now matching ground truth 5/6 with a legitimate near-tie on the 6th), end-to-end greedy generation on BOTH the original Q2_K file AND the Q8_0-isolated file still produces wrong output (Q2_K: repetitive 'â'/13276/40512 token loop; Q8_0: a different wrong sequence) -- NOT 'Paris'. FOLLOW-UP (same session): traced layer 2's expert selection the same way -- ALL 6 expert INDICES now match ground truth exactly ([3,62,63,35,38,27], just 62/63 swapped in reported order, which doesn't matter for a top-k SET), a full recovery from layer 1's 5/6. But the WEIGHTS have diverged much further: reference's top weight is 0.4421 vs this engine's 0.3385 -- a ~23% relative gap, up sharply from layer 1's ~0.5% weight gap despite the SAME code path.",
    "failure_scenario_continued": "SAME SESSION, FURTHER EXTENDED (all 26 layers, not just 0-2): generalized the trace (STINGRAY_MLA_TRACE=1) to print attn_norm/kv_cmpr/kqv_out/l_out sums and top-6 expert selections for EVERY layer, and extracted the matching ground truth for every layer from the llama-eval-callback dump via a fixed 1821-line-per-layer stride (verified exact). Two clean, decisive patterns emerged. (1) Expert-selection SET accuracy stays remarkably high through the ENTIRE 26-layer depth -- 17 of 26 layers match the top-6 expert SET exactly, the other 9 differ by exactly one expert (a marginal top-6-cutoff tie), with NO layer showing a wholesale wrong selection. This is strong evidence the MoE ROUTING mechanism itself (gating, top-k, the now-fixed norm/scale) is structurally correct throughout the network, not just at the layers directly inspected. (2) attn_norm's sum -- computed fresh each layer from RMSNorm(prev layer's l_out) + an F32 weight multiply, so it's a clean read of 'how wrong is the accumulated residual stream so far' -- diverges from ground truth SMOOTHLY and MONOTONICALLY: 0.00004% at L0 (exact), ~0.2-7% through L1-L3, climbing through 15-50% across L4-L18, crossing 80-92% by L19-21, and fully SIGN-FLIPPING from L22 onward. This is a textbook compounding-error growth curve (no discrete jump, no single bad layer), consistent with each layer contributing a small, roughly-constant absolute perturbation to the residual stream that accumulates additively across 26 sequential layers until it dominates a bounded-magnitude signal -- NOT consistent with a single discrete logic bug (which would typically show as a sudden discontinuity, not a smooth curve). CHECKED AND RULED OUT as an explanation for the divergence RATE (though fixed anyway as a real, independent gap): this engine's RmsNorm/RmsNormWide kernels (SimdKernels.cs) accumulated the sum-of-squares in float32, while ggml's reference (ggml-cpu/vec.h: `typedef double ggml_float`, used throughout ggml-cpu/ops.cpp's ggml_compute_forward_rms_norm_f32) accumulates in double. Fixed both kernels' final reduction (and the scalar tail/fallback paths) to widen to double, matching ggml exactly -- correct and now committed (all 626 OpenTail.Stingray.Tests.ForwardPass.Fast tests still pass), but empirically changed the attn_norm sums only in the 6th significant digit at every layer checked -- i.e. this was NEVER capable of explaining a 5-30%-per-layer divergence (a back-of-envelope check would have shown this: float32 accumulation error over ~2048 terms is ~sqrt(2048)*1.2e-7 ~= 5e-6 relative, four to five orders of magnitude too small), and confirms the true per-layer error source is elsewhere -- most likely the Q2_K/Q8_0 dequant-matmul kernels themselves (DotQ2K/SimdKernels.MatMulBatchedCached), given the Q8_0-vs-Q2_K isolation test in the entry above already showed quantization level directly and proportionally affects the per-layer error magnitude.",
    "conclusion": "Two real bugs found and fixed this session (expert_weights_norm/scale GGUF read+default, RmsNorm double-precision accumulation), both verified against ground truth and kept. The remaining gap to a passing greedy-parity receipt is now best explained as INHERENT NUMERICAL SENSITIVITY: this specific checkpoint (DeepSeek-V2-Lite, 15.7B params routed through top-6-of-64 sparse MoE with a single expert often taking >50% of the routing weight) compounds even small, per-layer, quantization-level numerical differences into a fully sign-flipped residual stream by layer ~22 of 27 -- a genuinely different failure mode than a discrete code defect, and NOT proven with certainty to be un-fixable, but three consecutive investigation rounds (two sessions) have now each found a real, independently-verifiable, non-root-cause bug rather than the alleged 'one more' fourth bug. Do NOT start a fourth round of formula-level or kernel-level code reading on a hunch -- the ground-truth-diffing methodology below is what has produced every real finding so far; extend IT, don't abandon it for more reading.",
    "next_steps": "RESOLVED THIS SESSION (do not re-attempt): tried the F16 isolation test suggested above. Force-requantized to F16 (--allow-requantize, 29.96 GiB) and re-ran the identical layer-by-layer trace. RESULT: F16's divergence pattern and magnitude is virtually IDENTICAL to Q8_0's (e.g. attn_norm at L8: 43.0% at F16 vs 34.8% at Q8_0; L21: 98.1% at F16 vs 92.0% at Q8_0) -- this DEFINITIVELY RULES OUT quantization precision as the driver of the compounding divergence (F16 is essentially lossless vs the Q2_K-derived values; if quantization noise were the cause, F16 should have shown dramatically less growth, not the same amount). This overturns this entry's own prior hypothesis. Given that, bisected attention vs FFN/MoE as instructed: added kqv_out (attention output, PER-LAYER, isolated from the accumulated residual) to the F16 trace. FOUND: kqv_out itself -- not just the accumulated attn_norm -- diverges early and sharply: 0.04% at L0, 0.6% at L1, then 5-20% by L2-3, a SIGN FLIP already by L12 (ours +2.35 vs ref -14.67), and 3-10x relative swings at several individual layers (L6, L9, L15) even while the SIGN still matches. This is far earlier and sharper than the accumulated-residual (attn_norm) view suggested, and happens independent of quantization level -- pointing at the ATTENTION computation itself (RoPE, the MLA q_nope/wk_b absorption, the causal softmax, or the weighted-V sum), not MoE/FFN (whose top-6 expert SET selection remained accurate throughout all 26 layers per the entry above) and not quantization (ruled out by the F16 test). Most plausible remaining explanation: attention's softmax is a SHARP, sensitive function of small score differences -- the same kind of near-tie sensitivity already demonstrated for MoE's top-6 cutoff (see the entry above) can occur in attention's score ranking too, and small differences from any tiny upstream computation choice (accumulation order, RoPE table construction, an FMA vs non-FMA dot product) can flip a close attention-weight ranking, amplifying rather than merely carrying forward the input's own small error. This is NOT proven to be inherent/unfixable rather than a discrete bug -- it was not isolated further within this session's remaining budget.",
    "next_steps_if_resumed_part2": "Ran both suggested checks above THIS SAME SESSION, with an important self-correction. (a) Reference determinism: reran llama-eval-callback TWICE on the identical Q8_0 file -- `diff` of every 'sum = ' line across both full 48225-line dumps: ZERO differences. The reference is 100% deterministic run-to-run; none of the measured divergence is reference-side noise, it is a genuine cross-implementation difference. (b) Bisected attention sub-stages by adding a new trace point for q_pe/k_pe SPECIFICALLY POST-ROPE (this class's MLA layout stores rope-rotated channels first per head, per MlaComputeQkv's doc comment, so this only needed summing the leading _ropeDim channels of the already-traced batchQ/batchK -- no new state). RESULT, WITH AN IMPORTANT CAVEAT: q_pe tracked reasonably well (2.7% at L4), but k_pe showed a 56% divergence at L4 specifically, which looked initially like a localized attention-path defect -- EXCEPT the reference's OWN k_pe sum at L4 (-24.96) is much SMALLER in magnitude than L0-L3's (76.6, -139.9, -152.8, 158.7), and k_pe is a raw signed sum over 384 terms (6 tokens x 64 dims) -- i.e. this is very plausibly catastrophic-cancellation sensitivity in the SUM METRIC ITSELF (a near-zero true value makes relative error explode even for an unremarkable absolute error), not evidence the computation is uniquely wrong at L4. L0-L3's k_pe actually tracked ground truth to within 0.1-10%, a normal range. This substantially weakens (does not eliminate) the earlier claim in this entry that attention's kqv_out diverging 'early and sharply' at L3-L12 specifically implicates attention over FFN/MoE -- several of those reference kqv_out values were ALSO small in magnitude (L4: 6.29, L6: 1.93, L9: -1.43), so that observation may be the same sum-near-zero artifact rather than a real early-attention defect. CORRECTED CONCLUSION: the one reliably interpretable signal remains the attn_norm curve (whose reference magnitudes stay large and non-cancelling, ~150-300, throughout all 26 layers) -- it shows a genuine, smooth, monotonic, quantization-independent divergence from an exact match at L0 to full sign-flip by L22. Everything else measured this session (kqv_out, k_pe, q_pe, per-layer sums generally) is real data but noisier and NOT reliable for sub-layer localization when the true value is near zero -- a lesson for whoever resumes: prefer L2-norm-of-difference or sum-of-|difference| metrics over raw signed sums for any further sub-layer bisection, since raw sums are only informative when the true magnitude is itself large.",
    "next_steps_if_resumed": "Ran the cancellation-proof re-check this SAME session, using sum-of-ABSOLUTE-values (sumAbs) alongside the raw signed sums, plus per-element token-0 samples (first3/last3) for direct comparison against the reference's own printed samples. FOUND, then INVESTIGATED, then RULED OUT as a new bug: l_out's sumAbs jumps ~20x between L2 (1508) and L3 (32923) -- initially looked like a smoking-gun discrete bug (a sudden jump, not smooth compounding). Checked the reference's OWN l_out-3 sample values: it ALSO jumps to the same huge magnitude at L3, specifically for TOKEN 0 (values like 14.4962, -5.3201, 8.5511 vs ~0.03-0.4 for every other token) -- i.e. this is a real, correctly-reproduced-by-both-implementations 'massive activation' phenomenon (well documented in the broader LLM literature: certain tokens develop outlier-magnitude activations at specific layers), NOT a defect. Added matching token-0 sample tracing to this engine's l_out trace and confirmed: at L3, ours=[-1.2831, 6.9063, 13.9057, ..., -4.8247, -5.0029, 7.6370] vs reference=[-1.8079, 7.5784, 14.4962, ..., -5.3201, -5.3390, 8.5511] -- same sign, same order of magnitude, ~10-15% relative drift, i.e. the SAME normal, moderate, already-characterized precision gap as everywhere else in this investigation, just made far more visible in absolute terms once the token's own magnitude is ~30-50x larger than a typical token.",
    "resumed_final_session_2026_08_21_evening": "User asked whether decomposing further against deepseek2.cpp's graph structure would help. Answer: no -- the graph structure was already verified correct earlier this session. But the user then added examples/ggml (the standalone, non-vendored ggml source, distinct from examples/llama.cpp/llama.cpp's copy), which gave direct access to ggml-cpu.c's type-trait dispatch table -- and THAT is where the real, definitive answer was found. First, re-verified the attention bisection with a corrected methodology: added a new trace point for post-softmax attention weights (SimdKernels.SoftmaxInPlace's output in PrefillCoreAttention, ForwardPass.Attention.cs) and re-ran the llama-eval-callback comparison with flash attention explicitly disabled (-fa off, needed because FLASH_ATTN_EXT is a single fused opaque node with no inspectable kq/kq_soft_max intermediates otherwise). CAUGHT AND FIXED AN OFF-BY-ONE IN THE DIAGNOSTIC SCRIPT ITSELF (not the engine): the reference dump's 'last causal token' row was being read from the wrong line, making L2/L3's attention weights look qualitatively wrong (different dominant position) when corrected extraction shows they in fact match the SAME ranking as this engine's output at every layer 0-4, with only the same ~5-30% quantitative drift characterized throughout this whole investigation -- attention is NOT a source of a discrete logic bug, full stop. THE ACTUAL ANSWER: checked examples/ggml/src/ggml-cpu/ggml-cpu.c's per-type CPU trait table (line ~298 for Q2_K, ~221 for F16, ~271 for Q8_0). Every quantized weight type is paired with a `vec_dot_type` that is ALSO reduced-precision: Q2_K/Q3_K/Q4_K/Q5_K/Q6_K weights pair with vec_dot_type=GGML_TYPE_Q8_K (meaning llama.cpp quantizes the F32 ACTIVATION to Q8_K first, then does an INTEGER Q2_K-times-Q8_K dot product via ggml_vec_dot_q2_K_q8_K); Q8_0-family weights pair with Q8_0 (activation quantized to Q8_0 first); F16 weights pair with F16 (activation ROUNDED to fp16 first, via ggml_vec_dot_f16). NONE of ggml's quantized-weight matmul paths ever do a full-F32-precision dot product against the raw activation -- every single one intentionally reduces the ACTIVATION's precision to match the weight's paired SIMD-friendly integer/half format, for throughput. This engine's MatVec/DotQ2K family does the opposite: it dequantizes the WEIGHT to float and dots directly against the FULL-PRECISION F32 activation, never touching the activation at all. This is not a bug in either implementation -- both are internally correct and self-consistent -- it is a genuine, PERMANENT, BY-DESIGN numerical-method difference present in every single matmul, in every layer, and it fully explains every observation this investigation made: why F16-requantized weights showed the SAME divergence pattern as Q8_0 instead of near-perfect precision (both still force an activation-precision reduction on the reference side -- fp16 rounding vs Q8_0 quantization -- that this engine's activation path never applies, regardless of the WEIGHT's own dtype); why the divergence compounds smoothly layer over layer (a small, systematic per-matmul precision difference, applied consistently 27 times over); and why every individual mechanism (RoPE, kq_scale, MLA reconstruction, MoE routing, RMSNorm) checked out as structurally and even numerically close, yet the aggregate still diverges -- the difference isn't IN any of those mechanisms, it's one level BELOW all of them, in the dot-product primitive itself.",
    "final_conclusion": "This is not a discoverable, fixable 'bug' in the traditional sense -- it is an intentional design divergence between this engine (prioritizes numerical accuracy: always dequantize-then-dot at full F32 activation precision) and llama.cpp/ggml (prioritizes throughput: always reduce activation precision to match the weight's paired SIMD dot-product kernel). Achieving true bit-for-bit greedy-parity with llama.cpp for ANY quantized architecture, not just deepseek2, would require deliberately REPLICATING ggml's activation-precision reduction (quantizing/rounding the activation to match each weight dtype's paired vec_dot_type before the dot product) -- a real, well-scoped, but substantial engineering task, not a bug fix, and one with a genuine tradeoff (matching ggml's precision-reduction would make EVERY quantized model's inference measurably LESS numerically accurate than this engine's current approach, in exchange for being greedy-parity-comparable to llama.cpp). Two real, independently-useful bugs WERE found and fixed this investigation regardless of this larger finding (expert_weights_norm GGUF default, RmsNorm double-precision accumulation) and should stay. Recommended path forward, in order of preference: (1) if greedy-parity-to-llama.cpp is a hard requirement for admitting new quantized architectures, implement matching activation-precision-reduction paths (e.g. an F16-activation dot for F16 weights, a Q8_K-activation dot for K-quant weights) as an opt-in 'llama.cpp-compatible' mode, and validate deepseek2 (and potentially re-validate other already-admitted quantized architectures) against it; (2) if bit-parity is not actually required and 'produces coherent, reasonable text' is the real bar, this specific checkpoint (DeepSeek-V2-Lite at low-bit quantization) may simply be an unusually sensitive case (16B params, top-6-of-64 sparse MoE, a documented massive-activation token) where this engine's higher-precision-by-design approach still doesn't converge to the same greedy tokens as llama.cpp's lower-precision one -- ship it behind --allow-unverified-arch with this full writeup as the documented caveat, or test on a larger/less pathologically-sensitive DeepSeek-V2/V3 checkpoint where the gap may not compound into a wrong first token."
  },
  {
    "file": "src/OpenTail.Stingray.Engine/ForwardPass.cs",
    "line": 1559,
    "summary": "Batched prefill (PrefillCore) diverges from token-by-token decode at specific, non-tile-aligned batch sizes, with every known approximate/quantized path ruled out as the cause. Found while triaging 7 pre-existing Tests.ForwardPass heavy-suite failures (2026-08-16); 5 of the 7 turned out to be stale test tolerances invalidated by later, intentional, default-on perf optimizations (Q8PrefillEnabled's repacked Q4K x8 kernel batching tokens differently depending on how a prompt was chunked, and a precision:2 rounding-boundary flake) -- fixed as test-only changes, not logged here. These 2 did NOT go away under the same treatment: tests/OpenTail.Stingray.Tests.ForwardPass/PrefillDecodeSelfConsistencyTests.cs's F32Prefill_MatchesTokenByTokenDecode(promptLength: 33) fails at cosine 0.997387 against a 0.9999 bound (lengths 2 and 8 pass) with Q8PrefillEnabled already pinned off by the test AND with the dequant-cache/BLAS path forcibly disabled too (prefillDequantCacheBytes: 0) -- identical failure value either way, so the divergence is not coming from matmul-kernel selection at all. The InlineData(33) comment already flagged '33 -- not a multiple of the 4/8-token dispatch groups' as the reason for picking that length, suggesting the original author suspected exactly this class of bug without confirming it. Root cause not located; PrefillCore's per-layer batched trunk (this line) and PrefillCoreAttention (line 3402) are the two places that treat N as tileable and are the natural place to bisect next, e.g. by comparing PrefillCore's per-token intermediate hidden state against RunTrunk/Forward's at each layer for a length-33 prompt to find where they first diverge.",
    "failure_scenario": "A 33-token real-model prefill (SmolLM2-1.7B-Instruct-Q4_K_M) run in one batched Prefill() call produces logits at cosine 0.997387 against the same tokens processed one at a time via Forward() -- a real, measurable divergence, not FP noise -- even with every known quantization/caching optimization disabled. Any production prefill at a batch size that lands wrong in whatever tiling boundary is responsible could silently generate degraded output with no error or warning."
  },
  {
    "file": "src/OpenTail.Stingray.Engine/ForwardPass.cs",
    "line": 1559,
    "summary": "Likely the same defect as the entry above, hit via the chunked-admission path instead of a single large prefill. tests/OpenTail.Stingray.Tests.ForwardPass/PrefillAttentionParityTests.cs's ChunkedPrefill_MatchesUnchunked_BothArmsBelowThreshold prefills 200 tokens in 64-token chunks (via PrefillWithCache, the actual continuous-batching admission path, issue #183) versus one unchunked pass; both arms stay under the 256-token Flash-64 attention threshold, so this is deliberately NOT a Flash-64 kernel-switch case (that's what the test exists to rule out, per its own doc comment). With Q8PrefillEnabled at its default, maxAbsDiff measures 0.76 -- suspiciously close to this codebase's own documented 'accepted' Q8-vs-F32 baseline (~0.807, see the Flash-64 threshold comment in this file around line 3480), so it looked at first like ordinary accepted Q8 noise. But pinning Q8PrefillEnabled off for the whole test class (matching the fix that resolved 3 sibling tests) does NOT clear it -- the failing value just changes, meaning a second, unrelated divergence survives underneath. Not yet bisected against the promptLength-33 defect above to confirm they share a root cause, but the shared symptom (survives every quantization-path toggle, tied to a batch size that isn't a clean multiple of whatever internal tile size is in play) makes that the leading hypothesis.",
    "failure_scenario": "A 200-token prompt admitted in 64-token chunks -- an entirely ordinary continuous-batching admission pattern, not an edge case -- produces logits meaningfully different (maxAbsDiff 0.76) from the same prompt prefilled in one pass, with no quantization path active to blame. This is a correctness gap in production's actual chunked-admission code path, not merely a test artifact."
  }
]
```

**Cut for the cap but still real and worth a look**, roughly in priority order: `DeepSeekMoeGraph.cs:172` (MoE `ExpertOffsets[MaxExperts]` off-by-index — silently drops expert gather/scatter on workspace reuse). Two candidates were explicitly REFUTED by verification: the `KvMemoryGovernor` TOCTOU race (safe — serialized through the session's own mutex with re-validation) and the `InferenceSession` double-release (safe — `Dispose()` is idempotency-guarded). Two more (`SpeculativeDecoder.cs` StepSampled/PLD bugs, `InferenceSession.cs` unguarded `Fork()` on CUDA) were confirmed as real defects but currently unreachable/latent — no wired call path exercises them yet.
