<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>3-tier memory hierarchy (VRAM → pinned RAM → NVMe) with SLRU expert cache and async prefetcher. Enables inference on models larger than VRAM.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\OpenTail.Stingray.Core\OpenTail.Stingray.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- MemoryHierarchy/Prefetcher are internal scaffolding (see MemoryHierarchy.cs); the Pipeline
         suite still needs to see them. -->
    <InternalsVisibleTo Include="OpenTail.Stingray.Tests.Pipeline" />
  </ItemGroup>

</Project>
