# OpenTail.Stingray

**Local LLM inference, multimodal vision, speech, and image generation, written in C#.** No Python, no P/Invoke to llama.cpp, no
sidecar process — the engine itself is managed .NET 10 code that runs in your process and
NativeAOT-publishes to a single binary.

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)
[![.NET 10](https://img.shields.io/badge/.NET-10-blue)]()
[![NativeAOT](https://img.shields.io/badge/NativeAOT-ready-green)]()
[![NuGet](https://img.shields.io/nuget/v/OpenTail.Stingray.svg)](https://www.nuget.org/packages/OpenTail.Stingray)

> **Built by [opentail.net](https://opentail.net)**

## What actually works today

A brutally honest status matrix, not a feature-list — and "verified" isn't one thing, so **Status** and **Confidence** are separate columns.

| Status | Meaning |
|---|---|
| 🟢 Correct | Produces the right output today |
| 🟡 Partial | Produces real output but with a known, open correctness gap |
| 🔴 Not there | Placeholder, unwired, or not attempted |

Confidence is *how* that was checked, since an exact numerical match, a golden token sequence, a perceptual listen, and "the model loads" are very different claims:

| Confidence | Meaning |
|---|---|
| 🔬 Golden-verified | Automated test compares against a real reference (exact tensor/logit match or cosine similarity threshold) |
| 👂 Working, listened/read | Real weights, full pipeline run end-to-end, a human (or transcription) confirmed the output is correct |
| ⚪ Implemented | Code exists and runs against real weights, not independently validated this pass |
| 🔵 Unexecuted | Real, non-stubbed implementation (reads genuine named weight tensors) that has never actually been run — no checkpoint downloaded/tested, so it's unknown whether it produces correct output, garbage, or crashes |
| 🔴 Placeholder | Not functional — stubbed, unwired, or synthetic data standing in for a real component |

Sourced from [`docs/audio-review-progress.md`](docs/audio-review-progress.md), [`docs/diffusion-samples/README.md`](docs/diffusion-samples/README.md), and [`docs/058-deepseek-full-lineage-implementation-plan.md`](docs/058-deepseek-full-lineage-implementation-plan.md); dates are when each was last verified.

| Capability | Status | Confidence | Backend | Notes |
|---|:---:|:---:|---|---|
| LLM inference (GGUF) | 🟢 | 🔬 | CPU / CUDA / Vulkan | Core engine — the most mature, most tested part of the project |
| MMS-TTS | 🟢 | 🔬 | CPU | Golden-verified against the real HuggingFace reference (2026-08-30) |
| XTTS-v2 | 🟢 | 🔬 | CPU | Voice cloning, 13 pieces individually golden-verified, wired end-to-end (2026-08-30) |
| Chatterbox-Turbo | 🟢 | 🔬 | CPU | Golden-tested (`ChatterboxCfmDecoderTests`), perf-optimized |
| Orpheus-TTS (3B + SNAC codec) | 🟢 | 🔬 | Vulkan / CPU | Real, working, timed 2026-09-10 (`OrpheusFullPipelinePerfBenchTests.cs`, 6 runs): transformer decode 14.4 tok/s mean, SNAC vocoder RTF 5.3x mean, ~10.0s/run total on Vulkan iGPU. Was previously only in this README's prose feature list with no badge row — added here for real, sourced credit |
| QwenTTS | 🟢 | 👂 | CPU | Was disabled after a QK-norm bug; fixed 2026-08-29 — verified via exact, word-for-word correct ASR transcriptions on two test phrases, not a numeric golden compare |
| Parler-TTS | 🟢 | 👂 | CPU | Two correctness bugs found & fixed by ear (2026-08-28) |
| F5-TTS | 🟢 | 👂 | CPU | RoPE bug root-caused and fixed (2026-08-28) |
| Fish Speech S2 Pro | 🟢 | 👂 | CPU | User-confirmed "sonically 100% spot on" (2026-08-29) |
| Whisper ASR | 🟢 | 🔬 | CPU | 100 languages. **2026-09-05**: confirmed and recorded the real end-to-end RTF impact of the native F16C GEMV kernel (landed and correctness-verified in an earlier session, but never re-measured end-to-end until now) — Medium 44.64s→10.32s (RTF 3.72→**0.86**, now faster than real-time), Large-v3 87.69s→19.13s (RTF 7.31→**1.59**), Base/Small ~4-4.4x faster too. Overturns the earlier "Medium/Large-v3 unambiguously too slow" finding. See `docs/audio-review-progress.md` |
| Piper TTS | 🟢 | 👂 | CPU | Fastest engine, 0.19× RTF; user-confirmed by ear (2026-09-03). Checkpoint not always resident locally (rotated for disk space per this project's own discipline) — that's routine, not a confidence downgrade |
| Kokoro | 🟢 | 👂 | CPU | Streaming; user-confirmed by ear (2026-09-03). Checkpoint not always resident locally (rotated for disk space) — routine, not a confidence downgrade |
| MeloTTS | 🟢 | 👂 | CPU | Streaming; user-confirmed by ear (2026-09-03). Checkpoint not always resident locally (rotated for disk space) — routine, not a confidence downgrade |
| CosyVoice 3 | 🟢 | 👂 | CPU | **Root-caused and fixed 2026-09-06**: DiT attention was applying RoPE to all 16 attention heads instead of only head 0 (confirmed directly against `ggml_rope_ext`'s real documented n_dims-vs-tensor-width semantics in the real `examples/audio.cpp` reference) — same bug class already fixed in F5-TTS. Fix verified via real per-mel-channel variance comparison against the built-from-source reference on two independent text/seed samples (all 80 channels converged to within 0.89x-1.28x of the reference, down from up to 2.8x); **user-confirmed by ear 2026-09-06: "cosyvoice3-OURS-gen2.wav is perfect."** A separate frozen-ODE-noise fix and prompt/target frame-alignment fix landed the same day as real, necessary but not individually sufficient, prerequisites — see `docs/audio-review-progress.md` |
| CosyVoice 2 (0.5B) | 🟢 | 👂 | CPU | Shares CosyVoice 3's fixed DiT attention/ODE/frame-alignment code paths (same underlying pipeline, smaller LLM backbone) — see `docs/audio-review-progress.md` |
| RVC (Retrieval-based Voice Conversion) | 🟡 | 🔬 | CPU | **In progress, 2026-09-06**: HuBERT content encoder and RMVPE pitch extractor both golden-verified against the real C++ reference (several real structural bugs found+fixed along the way — audio-padding preprocessing, a U-Net H/W-axis orientation swap, a `conv_block_res` ReLU-placement bug; RMVPE's remaining small gap was root-caused to a latent quirk in the *reference's own* ggml graph wiring, not this port). The VITS-style synthesizer (relative-position-attention text encoder, normalizing flow, NSF-HiFiGAN generator) is now golden-verified end-to-end too — fed the reference's own real content/pitch/noise inputs from a real audio run and compared every intermediate: the final waveform matches the reference to floating-point precision (maxAbsDiff ≈ 0.00003 across 317,600 samples). The k-NN retrieval blend is also implemented and tested. Still missing for a complete callable pipeline: `native_pipeline.cpp`'s orchestration layer (chunk splitting, RMS mix, pad-crop). See `docs/audio-review-progress.md`'s RVC section |
| Voxtral Realtime (ASR) | 🟢 | 🔬 | CPU | **Full pipeline golden-verified, 2026-09-06**: mel frontend, audio tower, real padding/prompt construction, the text decoder (Mistral GQA plus a real additive audio-embedding splice and an AdaLN-Zero-style delay-conditioning gate, NOT a plain drop-in), and now a real KV-cache incremental autoregressive generation loop all work together end to end on real audio and real weights — the decoded transcription is an **exact, word-for-word match** against the reference's real output ("This little work was finished in the year 1803, and intended for immediate publication."). Known minor gaps: a small prefill `audio_tokens` off-by-one (124 ref vs 123 this port, doesn't affect the transcription match) and no real EOS-based early stopping. See `docs/audio-review-progress.md`'s Voxtral section |
| Nemotron 3.5 ASR (FastConformer-RNNT) | 🟢 | 🔬 | CPU | **Full pipeline working, 2026-09-06**: real mel frontend (magnitude spectrum, no per-feature normalization — a real, deliberate difference from Parakeet's pipeline), a real causally-padded `dw_striding` subsampling front-end, a 24-layer Conformer encoder with Transformer-XL relative-position attention under a real CHUNKED (not plain causal) attention mask, and a 2-layer LSTM RNNT predictor + joint network with a real frame-synchronous greedy decode (including the reference's "skip predictor update on repeated blanks" optimization) all work together on real audio and real weights — produces an essentially correct transcription ("This little work was finished in the year eighteen oh three and intended for immediate publication. \<en-US\>") on the same clip Voxtral was verified against. Not yet done: numeric golden-verification against a captured reference trace (this is a real, coherent match by eye, not yet byte-for-byte), and the streaming/incremental decode path. See `docs/audio-review-progress.md`'s Nemotron ASR section |
| OmniVoice (TTS) | 🟡 | 🔬 | CPU | **2026-09-07**: the full DAC-style acoustic codec (encoder AND decoder), a HuBERT semantic encoder, and OmniVoice's OWN full real reference-audio ENCODE chain are all real and wired. OmniVoice's own generation loop — a genuinely self-contained (NOT the shared `IForwardPass` engine), real MaskGIT/SoundStorm-style transformer with fully non-causal full self-attention and real classifier-free-guidance dual batching — is now implemented (`OmniVoiceMaskGitGenerator`) and produces real, in-range codes on the first real attempt. Missing: wiring a real tokenizer-driven prompt (this pass used synthetic token ids), numeric golden-parity against the reference. See `docs/audio-review-progress.md`'s OmniVoice sections |
| MOSS-TTS-Nano | 🟡 | ⚪ | CPU | **Full pipeline wired end-to-end, 2026-09-07**: a real SentencePiece BPE text tokenizer (hand-parsed from the checkpoint's raw protobuf `.model`, confirmed BPE not Unigram before implementing), the GPT2+RoPE global transformer, the per-codebook-autoregressive local frame decoder, the real zero-shot prompt template, the multi-frame generation loop, and the "CNN-free" Transformer-stack RVQ audio codec decoder are all implemented and real-weight verified (finite/in-range/causal/non-collapsing invariants, not numeric golden-parity — no captured Python reference trace exists for this checkpoint). Produces a real, non-crashing 48kHz stereo waveform from raw text; not yet evaluated for perceptual audio quality (no listening pass done this session). Missing: `precompiled_charsmap` real normalization, sampling beyond greedy/argmax, voice-cloning reference-audio prompts, and numeric golden verification against a real oracle. See `docs/audio-review-progress.md`'s MOSS-TTS-Nano sections |
| PersonaPlex (duplex speech LM) | 🟢 | ⚪ | CPU | **Live-duplex generation wired end-to-end 2026-09-08**: complete real pipeline in BOTH directions — temporal LM, Depformer, and the real Mimi neural codec DECODE path (real multi-stream delay pattern, real temperature/top-k sampling) plus the Mimi codec's ENCODE direction (waveform → RVQ codes, real per-frame nearest-code RVQ), all real-weight-verified and chained end-to-end. `PersonaPlexGenerator.GenerateWithUserAudio` drives the real `run_user_frame`-equivalent loop with genuine user audio in, genuine generated audio out — the first real duplex-conversation run in this project (25.48 GiB weights, 40.4s, first attempt succeeded). Real voice-id-conditioned bootstrap and system-prompt text are also wired. Missing: numeric golden-parity against a captured reference duplex run, real streaming Mimi decode/encode (both directions currently one-shot). See `docs/audio-review-progress.md`'s PersonaPlex sections |
| VoxCPM2 | 🟢 | 👂 | CPU | **RESOLVED 2026-09-08**: the earlier "gets confused quickly" listening-check defect was root-caused to two real bugs — a missing tokenizer `Prepend`-normalizer detection (first token of every prompt was wrong) and, more significantly, `"minicpm"` missing from `ModelGraph.cs`'s NEOX-RoPE architecture switch (`"minicpm3"` was listed but not the plain dense `minicpm` architecture VoxCPM2 uses, so RoPE was applied LLaMA-style-interleaved instead of NEOX split-half). Found via a real per-layer hidden-state bisection against the vendored C++ reference (new `EnableHiddenTaps`/`HiddenTapsAt` reuse, zero engine changes) — post-fix, all 40 sampled points across all 28 layers match the reference with zero sign flips, residual ~1-8% noise consistent with normal Q8_0 accumulation differences. **User-confirmed by ear 2026-09-08: "voxcpm2-real-check.wav is good."** Real clean perf comparison against a freshly-generated C++ reference sample (same prompt/seed): ours is RTF 5.39 vs the reference's RTF 3.56, ~1.5x slower — a modest CPU-path gap, not a crisis. Every major piece (`base_lm`, `residual_lm`, local encoder, CFM/DiT solver, AudioVAE encoder AND decoder) is real, real-weight verified, and now numerically correct; both the zero-shot and voice-cloning/reference-audio-conditioned loops run end-to-end. Missing: real streaming variant. See `docs/audio-review-progress.md`'s VoxCPM2 sections |
| Higgs Audio TTS | 🟡 | ⚪ | CPU | **2026-09-07**: real Qwen3-family AR backbone (discrete audio-codebook tokens embedded via a separate, shared modality-embedding table rather than spliced as extra vocab rows), real delay-pattern state machine (`HiggsCodebookSampler`, plus a newly-ported `HiggsCodebooks` delay/reverse-delay helper), real temperature/top-k/top-p sampling, and the real reference-audio ENCODE chain (`HiggsCodecEncoder`) are all real-weight verified — and now BOTH the zero-shot AND the reference-audio-conditioned (voice-cloning) full text-to-waveform generation loops (`HiggsGenerator.Generate`/`.GenerateWithReferenceAudio`) are wired end-to-end, each passing its real-weight smoke test on the first attempt. Missing: KV-cache reuse across repeated same-reference calls (perf only), numeric golden-parity against the reference. See `docs/audio-review-progress.md`'s Higgs Audio TTS sections |
| VibeVoice TTS | 🟡 | 👂 | CPU | **2026-09-08, real bug found+fixed**: the diffusion head had been conditioned on `IForwardPass.LastHidden` (this engine's documented PRE-final-norm hidden state) instead of the POST-final-norm value the real reference actually uses (`decoder.cpp`'s `hidden_output_`, confirmed captured AFTER its own `RMSNormModule` call at 4 separate real graph-build sites) — affected every diffusion-sampling step of every generated frame. Fixed with a new `VibeVoiceLlmTensorSource.NormWeight` accessor + an RMSNorm applied before use at all six call sites; all four affected real-weight tests still pass, full suite clean. The identical bug (same `LastHidden` misuse pattern) was independently found in `PersonaPlex` around the same time — worth auditing every other model that reads `LastHidden` for the same mistake. Not yet human-confirmed by ear post-fix. See `docs/audio-review-progress.md`'s latest VibeVoice TTS entry |
| VibeVoice TTS | 🟡 | ⚪ | CPU | **2026-09-07**: real interleaved text/diffusion generation loop (LLM emits control tokens; on `speech_diffusion`, a real CFG diffusion sampler produces one acoustic-latent frame, decoded/re-encoded through the real acoustic+semantic codecs) chained end-to-end and real-weight verified, with real temperature/top-k/top-p sampling for the control-token stream. Real voice-cloning (`Voice input:` prompt section, real per-speaker acoustic-latent splicing via `VibeVoiceGenerator.GenerateWithVoiceCloning`) is now also wired end-to-end and real-weight verified. Deliberate simplification: uses one-shot (non-streaming) codec decode/encode per chunk rather than the reference's real stateful streaming state. See `docs/audio-review-progress.md`'s VibeVoice TTS sections |
| VibeVoice ASR | 🟡 | 👂 | CPU | **Runs end-to-end 2026-09-08 but NOT yet transcription-correct**: RAM-ceiling blocker resolved, real greedy decode loop wired. A real per-op bisection (`STINGRAY_ASR_TRACE` taps in the reference + a matching C# tap on `VibeVoiceTokenizerEncoder.Encode`) traced a real numeric divergence all the way back to the resampled input waveform itself; ruled out `AudioResampler` as the cause (a frequency sweep + broadband-tone test showed unity gain everywhere) and instead found a genuinely missing preprocessing step — the reference's `VibeVoiceASRFrontend::normalize` RMS-normalizes audio to a target dBFS after resampling, which this port never did. Added `VibeVoiceAudioNormalizer` and wired it in: closed the raw-waveform divergence from ~52% off to ~8% off, a real and substantial fix — but the transcript is still garbled, so either the remaining ~8% is still enough to break the LLM's sensitive decode, or a second bug remains elsewhere (not yet isolated). Missing: re-running the bisection with the normalization fix applied to find what's left, numeric golden-parity, real temperature/top-p/top-k sampling (argmax-only so far), beam search. See `docs/audio-review-progress.md`'s VibeVoice ASR sections |
| NeuTTS | 🟢 | 🔬 | CPU | **NEW model, full pipeline ported and wired end-to-end this session, 2026-09-08**: a real Qwen3-family backbone LM (`NeuTtsBackboneTensorSource`, bridged into the existing `ForwardPass` with zero bespoke transformer math), a real AR speech-token generation loop (`NeuTtsGenerator`/`NeuTtsPromptBuilder` — speech-codec tokens are genuinely native vocabulary ids in this checkpoint, `<\|speech_0\|>`..`<\|speech_65535\|>`, no splicing trick needed), and a real FSQ acoustic-decoder codec (`NeuTtsAudioDecoder` — FSQ dequant, ResNet+Transformer stack with real NEOX RoPE, Vocos-style ISTFT head). Every stage's first real attempt succeeded, no debugging needed. Real bug found in this project's shared tokenizer infra along the way (not NeuTTS-specific): `HuggingFaceTokenizerSource` doesn't surface every `added_tokens` entry from a large `tokenizer.json`; worked around with a small direct JSON reader rather than touching already-verified shared code. Missing: numeric golden-parity, voice-cloning from arbitrary (non-preset) reference audio (needs the encode-direction semantic branch, out of scope). See `docs/audio-review-progress.md`'s NeuTTS sections |
| MOSS-TTS-Nano | 🟡 | 👂 | CPU | Real temperature/top-k/top-p sampling for the per-frame audio-codebook path added 2026-09-07 (`MossTtsLocalFrameDecoder`), on top of the already-wired full pipeline (see the entry below for the base pipeline description). **2026-09-08, real bug found+fixed after a user listening check flagged the output as bad**: every real sample this session had generated was greedy-decoded (argmax), not sampled — fixed to use the reference's real sampling defaults (`temperature=1.7, top_p=0.8, top_k=25`), closing the spectral-content (ZCR) gap from ~54% off to ~7% off the reference. **The remaining ~2.7x loudness gap was then fully root-caused and formally accepted**: fed the reference's own exact generated codes (captured via a real trace hook) directly into this port's codec decoder — RMS/peak matched the reference essentially exactly, PROVING the codec decoder itself is fully correct. The gap is entirely in code generation: the reference's sampler draws from `std::mt19937`/`std::discrete_distribution`, this port's from .NET's `System.Random` — a fundamentally different, non-bit-reproducible RNG (same accepted-gap class as `VibeVoiceAcousticLatentSampler`'s RNG mismatch), not a fixable defect. See `docs/audio-review-progress.md`'s MOSS-TTS-Nano sections |
| Qwen3 Forced Aligner | 🟢 | ⚪ | CPU | **RESOLVED 2026-09-07**, closing a bug open across multiple sessions: 6 real audio-encoder bugs fixed first (positional-embedding sin/cos layout, transposed conv2d input orientation, tanh-vs-exact-erf GELU, missing peak-clip-guard/min-samples preprocessing, wrong FFT size, missing Whisper-style centered/reflect-padded framing — encoder correlation 0.97-0.99), then the actual root cause: `ForwardPass` was silently running without per-head QK-RMSNorm (`hp.HasQkNorm` evaluated `false` because `QwenAsrLlmSafetensorsTensorSource` never set the `has_qk_norm` metadata flag the GGUF-loading path sets automatically, and the aligner didn't pass a `tensorSource` for `FromGgufMetadata` to auto-probe it either). Fixed and independently re-verified: real reference audio now produces strictly monotonic, sensible word-level timestamps end to end. See `docs/audio-review-progress.md`'s many Qwen3 Forced Aligner sections dated 2026-09-07 |
| MarbleNet VAD | 🟢 | 🔬 | CPU | **NEW model, ported from scratch and GOLDEN-VERIFIED on the first attempt, 2026-09-08**: a small NVIDIA NeMo "Jasper"-family depthwise-separable-CNN voice-activity detector, using a checkpoint bundled directly in this repo (466KB, no download). Real log-mel features (checkpoint's own shipped window/filterbank) through 6 Jasper blocks (BatchNorm folded into conv weights at load time) into a per-frame 2-class decoder. One real bug caught while porting (not after): initially folded BatchNorm into both the depthwise AND pointwise convs of each separable repeat — the reference only folds it into the pointwise conv, the depthwise conv is BN-free. Verified against the vendored reference CLI's real output on the same LibriSpeech clip: exact sample-bound and confidence match (`{"start_sample":8320,"end_sample":56320,"confidence":0.977519}`). Missing: a public pipeline wrapper, real streaming mode, a real negative (silence-only) test case |
| Citrinet ASR | 🟢 | 🔬 | CPU | **SECOND new model ported from scratch this session, GOLDEN-VERIFIED on the first attempt, 2026-09-08**: NVIDIA NeMo's larger Citrinet CTC "Jasper"-family CNN — 23 real blocks (parsed directly from the checkpoint's embedded config JSON, too many to hand-transcribe), each with a real Squeeze-Excite gate, real per-feature Z-normalized log-mel input, greedy CTC decode, and a new minimal decode-only SentencePiece vocabulary reader. Real downloaded checkpoint (`audio-cpp/audio.cpp-gguf`, 38.7 MiB). Verified against the vendored reference CLI's real output on the same real LibriSpeech clip used for MarbleNet VAD: both sides produce the exact same transcription, `"concord returned to its place amidst the tents"` — a correct transcription match, not just non-degenerate output, on the first real attempt. Missing: a public pipeline wrapper, a second real test clip, a real negative/no-speech case |
| SenseVoice (ONNX) | 🟢 | 🔬 | CPU | **NEW, real ONNX pipeline built and GOLDEN-VERIFIED on the first attempt, 2026-09-11** (`src/OpenTail.Stingray.Audio/SenseVoice/`): real 4-input ONNX forward pass, real metadata read from the checkpoint's own embedded ONNX custom metadata (added `OnnxModelSession.CustomMetadata`), real fbank+LFR+CMVN feature extraction (reused `FunAsrRealMelExtractor` unmodified, parameters confirmed matching via the real sherpa-onnx exporter script), real CTC greedy decode, real vocab downloaded from the matching HF repo. Verified on the same real LibriSpeech clip Citrinet-ASR used: **exact transcript match**, `"concord returned to its place amidst the tents"`, plus correct language/emotion/event tags. Was previously 0% implemented — only a doc-comment mention existed |
| FunASR Paraformer (ONNX) | 🟢 | 👂 | CPU | **NEW, real, independent ONNX pipeline, verified with real Mandarin speech 2026-09-11** (`src/OpenTail.Stingray.Audio/ParaformerOnnx/`), separate from the still-broken native GGUF conversion (missing `pf.vocab` metadata). Real metadata directly inspected from the checkpoint's own ONNX custom metadata, real single-graph forward pass (the whole encoder+CIF-predictor+decoder is fused into one ONNX export), real per-position-argmax-with-EOS decode, real downloaded vocab (confirmed matching this exact checkpoint). **Verified on a real Chinese speech clip** (from the same HF repo the vocab came from): a real, coherent, grammatically valid Mandarin transcript — not an exact ground-truth string match, but unambiguously real, sensible speech content |
| Stable Diffusion 1.5 | 🟢 | 👂 | CPU / Vulkan | **First real measurement 2026-09-11**: real, coherent 512×512/20-step generation (a genuine photorealistic scene, not degenerate) — see [diffusion samples](docs/diffusion-samples/README.md) |
| SDXL-Turbo | 🟡 | 👂 | CPU / Vulkan | **First real measurement 2026-09-11**: at 4+ steps produces real, coherent images (a genuine apples-on-a-table scene). Found and fixed a real `EulerDiscreteScheduler` divide-by-zero at `--steps 1` that previously produced a solid-black image (not an SDXL-Turbo-specific bug — affects any checkpoint run at 1 step, fix benefits all of them). **Still 🟡, not 🟢**: this checkpoint's actual selling point (1-2 step generation) produces low-quality/textured-noise output even post-fix — the scheduler only implements "linspace" timestep spacing, not the "trailing" spacing real turbo-distilled models need for good few-step results, so 1-2-step SDXL-Turbo specifically should not be read as working, only "no longer crashes" |
| Z-Image-Turbo | 🟢 | 👂 | CPU / Vulkan | **Fixed 2026-09-01**: `QwenTextEncoder`'s GPU path unconditionally uploaded BF16 tensors regardless of backend support; `VulkanBackend.Sgemm` had no BF16 fallback and silently reinterpreted the 2-byte BF16 buffers as 4-byte FP32, corrupting Qwen3's text embeddings to NaN before the DiT ever saw them. Now gated on `BestSgemmPrecision==Bf16`; real recognizable apple image confirmed on the Vulkan GPU path |
| Wan 2.1 / 2.2 Video | 🔴 | 👂 | CPU / Vulkan / CUDA | Five real DiT/VAE bugs fixed (RoPE, QK-norm, unpatchify, VAE scaling, VAE upsample), a further ~6x CPU perf improvement landed, and a real 256x256/12-step run (past the point where VAE-resolution artifacts confound the result) still shows a strong, regular grid artifact — this disproves the earlier "just needs more steps" theory. Every specific hypothesis checked so far (RoPE axes, AdaLN modulation, flow-schedule/CFG formula, GELU, checkpoint tensor shapes) is individually ruled out; the real remaining bug is still unlocated. See `docs/diffusion-samples/README.md` for the full handoff (2026-09-01) |
| LTX-Video | 🟡 | 🔬 | CPU | **2026-09-02**: real, checkpoint-driven port — DiT transformer, VAE decoder, the real multi-step scheduler+CFG denoising loop, and T5-v1.1-XXL encoder+tokenizer are each independently golden-verified at effectively machine precision (>0.999999 cosine similarity) against their real reference implementations; one real bug found and fixed (missing per-channel VAE latent normalization). Real end-to-end 256x256 generations initially showed heavy per-pixel corruption, DEFINITIVELY traced to the checkpoint being run far below its trained resolution regime (model card: 1216x704 flagship, "works best under 720x1280"), not this port — the actual unmodified official `LTXVideoPipeline` class produced the identical corruption at 256x256 too. Re-running both the real pipeline and this C# port at 512x512 with one specific matched prompt/seed/steps/CFG resolved it: both independently converged on a real, recognizable image that day (`docs/diffusion-samples/ltx-video-python-reference-512.png`, `docs/diffusion-samples/ltx-video-csharp-port-512.png`). **Downgraded to 🟡 2026-09-11**: a broader sweep (6 runs total — 3 at 256×256, 2 more at 512×512, spanning both the default random seed and an explicit `--seed 42`) found only 1 of 6 produced a coherent image; the other 5 (including a repeat of the exact 512×512 config) produced pure visual noise. Not yet root-caused (golden-parity tests for every individual component — RoPE, VAE decode, T5 encoding, single-step trajectory — all still pass real and clean, so the bug isn't isolated there); real convergence appears to depend heavily on the specific seed/config in a way the original 2-seed comparison didn't surface. See `docs/055-ltx-video-implementation-plan.md` for the original investigation and `docs/00-current-work.md`'s 2026-09-11 entry for the newer, contradicting findings |
| HunyuanVideo | 🔴 | ⚪ | — | DiT runs clean through every layer with real weights (2026-08-31 smoke test) — blocked on a real VAE decoder (needs its own class, same as Wan) and real dual CLIP+LLM text conditioning, neither wired yet |
| FLUX.1 | 🟡 | 👂 | CPU | **Run for the first time 2026-09-01** (`FLUX.1-schnell`, real checkpoint): six real bugs found and fixed across two rounds — a GGUF tensor-name prefix mismatch, two missing `.weight` suffixes, GEGLU-vs-plain-GELU in the MLP, a full 2D RoPE rewrite (wrong axis split + wrong rotation convention), a flow-matching Euler integration sign inversion, and a `[txt,img]`-vs-`[img,txt]` token-ordering bug — each confirmed against the real `black-forest-labs/flux` source, not guessed. **2026-09-11: major real bug found and fixed** — `SingleBlock`'s attn+MLP output was concatenated as two whole-sequence blocks instead of the real reference's per-token feature-axis interleave, corrupting `linear2`'s input on every one of the 38 single blocks, every token, every forward call. Fixed (real reference re-checked line-by-line first — QKV/RoPE/RMSNorm wiring all re-confirmed clean, this was one stage downstream of there). **Verified with a real end-to-end run: for the first time ever, a genuinely recognizable red apple and wooden table render correctly** — a major, structural improvement from every prior round's pure noise/fully-tiled output. Upgraded 🔴→🟡. **Not fully fixed**: a repeating tiled pattern still dominates the background/upper-left portion of the same image — a second, real, still-open, now-separable artifact. See `docs/056-flux-tiling-artifact-handoff.md`'s Round 7 |
| SD3/3.5 | 🟡 | 👂 | CPU / Vulkan / CUDA | **Run for the first time ever, 2026-09-02**, five real bugs found and fixed (fused-QKV layout, missing QK-RMSNorm, missing AdaLN SiLU gating, unimplemented dual-attention MMDiT-X extension, unhandled `context_pre_only` modulation) — see `docs/057-sd35-performance-handoff.md`. **2026-09-05: real image output achieved** — found and fixed FOUR more real bugs via direct `examples/diffusers` source comparison: dual-attention blocks normalized the wrong (post-residual) input for their second attention pass; SD3.5's VAE was silently decoded with FLUX/Z-Image-Turbo's scale/shift constants (a different 16-channel VAE checkpoint, ~4.24x scale error); unpatchify assumed the wrong per-patch channel/spatial flattening order (patchify and unpatchify use genuinely different, non-symmetric conventions); and — the big one — no positional embedding was ever added to image patch tokens at all. Iterating through these in order, the 256×256/20-step disambiguation image went from pure color noise → unchanged → unchanged → a uniform periodic weave texture (the classic "no positional info" signature) → **real, coherent, asymmetric image structure** (distinct shapes, edges, shading, reflections) — not yet a clean photorealistic match to the test prompt, but unambiguously a real generated image, not noise, by a wide margin the furthest this checkpoint has ever gotten. Not yet numerically golden-verified against a reference. See `docs/057-sd35-performance-handoff.md` |
| Stable Audio 3 (Small Music) | 🟡 | 👂 | CPU | **Revised down 2026-09-03** from an earlier "fully ported and confirmed" claim — that judgment leaned on a single 6s ear-check and didn't account for a real gap found this session (below), so it no longer meets this matrix's own bar for 🟢. Real DiT/VAE/T5Gemma are weight-driven and golden-verified against the real HF reference at a (numerically chaotic) 0.5s fixture; a 6s listening check was user-confirmed "100% good" (2026-09-02). **2026-09-03**: found the real reference pads every generation with 6s of latent headroom, masks attention over it, and warps the Euler timestep schedule via a real `DistributionShift` formula (`use_effective_length_for_schedule`/`mask_padding_attention`, confirmed from the real per-checkpoint `model_config.json` and GitHub `main` source) — none of which this port implemented until just now (`StableAudioScheduleKernels.cs`); attention masking specifically is still not wired (documented gap). Not yet re-confirmed by ear post-fix — see docs/066 |
| Stable Audio 3 (Small SFX) | 🟡 | 👂 | CPU | Byte-identical DiT/VAE config to Small Music (different trained weights only) — but the operator judged the `sa3_small-sfx_glass-shatter_3s.wav` sample poor quality (2026-09-03), unlike Music. Real CFG/APG formula independently re-verified against GitHub `main` source and found correct (not the bug). The duration-padding/schedule-shift fix above is now applied; a regenerated sample is pending the operator's listen — since the same gap was present in Music's "good" sample, it may not be the SFX-specific cause; root cause not yet confirmed either way. See docs/066 |
| Stable Audio 3 (Medium) | 🟡 | 👂 | CPU | Real differential-attention DiT + SAME-L VAE (genuinely different from Small, not a config reuse). **Fixed and CONFIRMED 2026-09-03**: the real, deliberately-deferred `sinusoidal_blocks` VAE-decoder FeedForward variant (GLU gate activation `Sin(x)=sin(pi*x)` on the last 7 of 12 decoder layers, real formula from GitHub `main`) was the cause of a reported poor-quality sample — a regenerated sample was operator-confirmed good, the original confirmed not good. The same duration-padding/schedule-shift gap as Small also applies here and is now fixed; numeric golden-parity for the DiT/VAE is still not done. See docs/057/066 |
| MiniMax-Music3 | 🟢 | 👂 | CPU / Vulkan (GPU path currently slower than CPU on this machine's iGPU — see `docs/066`) | **Fixed and user-confirmed 2026-09-04/05** — all six real components (condition encoder, Qwen3-based language model, RVQ depth decoder, flow-matching DiT transformer, vocoder, tokenizer) individually golden-verified against the real `diffusers` reference. A real end-to-end listening check surfaced composed-pipeline bugs that no single component's golden-parity test could catch: diffed the full pipeline against the independent `minimaxmusic.cpp` C++ reference and found (1) the flow-matching Euler scheduler ran its sigma schedule in the wrong direction (descending instead of the reference's ascending), and (2) residual-codebook CFG sampling used the semantic head's cond-first top-k ranking instead of the reference's guided-first ranking, affecting 7 of every 8 sampling decisions per frame. Both fixed; **user confirmed the resulting audio is correct**. A CPU performance pass in the same session (depth-decoder KV cache, batched CFG branches, vectorized linear layers, vocoder tiling/FMA) cut a 200-frame (~8s) generation from ~56-58 minutes to 582.9s (~9.7 min). See `docs/066-minimax-music3-future-plan.md` |
| MusicGen (text-to-music) | 🟢 | 🔬 | CPU | **2026-09-02: first real end-to-end generation** (`facebook/musicgen-small`, T5 conditioning + 24-layer delayed-pattern decoder + CFG + EnCodec 32kHz decoder), non-degenerate audibly musical output (user-confirmed "I can hear music in the sample"). Two real bugs found and fixed then: a delay-pattern input/target off-by-one, and a missing `enc_to_dec_proj` (768→1024) projection. CFG null-condition convention independently confirmed by reading AudioGen's real `audiocraft` source. **2026-09-04: real numeric golden-parity closed across the FULL pipeline** — four pure-numpy oracles transcribed from real HF `transformers` source against the real checkpoint (`scratch-llamacpp-ref/musicgen_{decoder,t5_encoder,encodec_decoder,e2e}_golden.py`) verify, respectively, the decoder transformer, the T5 text encoder, the EnCodec 32kHz decoder, and the full chained pipeline via the actual public `MusicGenGenerator.Generate` entry point (including an independent tokenizer-drift check). All four pass, cosine similarity > 0.999 each, zero production bugs found — every stage's existing C# already matched the real reference math exactly. **2026-09-05: real 2.99x end-to-end speedup** — the 24-layer decoder's Q/K/V/O/FFN/LM-head weights were plain F32; wired through the native F16C kernel already proven on Whisper's decoder (same single-token incremental-step shape), real generation smoke test 113.8s→38.1s, correctness re-confirmed. See `docs/062-musicgen-implementation-plan.md` |
| AudioGen (text-to-sound) | 🟢 | 🔬 | CPU | **2026-09-02, same day as MusicGen: first real end-to-end generation.** `facebook/audiogen-medium` (48-layer delayed-pattern decoder, same architecture family as MusicGen, + external `t5-large` conditioning + a separately-trained 16kHz environmental-sound EnCodec) runs against real weights and produces non-degenerate audio. No official HF `transformers` release exists for this model — converted the real native AudioCraft `.bin` checkpoints to safetensors via this environment's Python/torch rather than a hand-rolled pickle parser; every hyperparameter came from the checkpoint's own embedded training config, cross-checked against the real `audiocraft` package's source. Proved MusicGen's delay-pattern and CFG-formula code genuinely reusable unchanged; extracted two new shared kernels (`Primitives/T5EncoderKernels.cs`, `Primitives/EncodecDecoderKernels.cs`) now used by both models. **2026-09-04: real numeric golden-parity fully closed for all three real stages, not just the decoder** — `AudioGenDecoderGoldenParityTests` (decoder, cosine > 0.999), `AudioGenTextEncoderGoldenParityTests` (real stock `t5-large` text conditioner via the shared `Primitives/T5EncoderKernels.cs`, cosine > 0.999, `scratch-llamacpp-ref/audiogen_t5_large_golden.py`), `AudioGenEncodecDecoderGoldenParityTests` (real 16kHz EnCodec decoder, cosine > 0.999, `scratch-llamacpp-ref/audiogen_encodec_decoder_golden.py`), and `AudioGenEndToEndGoldenParityTests` (full chain: real T5-large encode -> real greedy delayed-multi-codebook decoder generation -> real EnCodec decode, cosine > 0.999, `scratch-llamacpp-ref/audiogen_e2e_golden.py`) all pass cleanly against production code with real weights, no further bugs found. **2026-09-05: real 3.68x end-to-end speedup** — same fix as MusicGen (48-layer decoder's weights were plain F32, now native-F16C via the same shared kernel), real generation smoke test 962.9s→261.4s, correctness re-confirmed. See `docs/063-audiogen-implementation-plan.md` |
| DeepSeek-V2/V3/R1 (`deepseek2`) | 🟡 | ⚪ | CPU | Loads, prefills, and decodes real weights (DeepSeek-V2-Lite-Chat, Q2_K and native Q8_0) with zero crashes across 27 layers, but produces numerically wrong greedy output — closed investigation (`docs/done/032-deepseek2-mla-yarn-moe-routing-investigation.md`) found and fixed 8+ real bugs (YaRN/kq_scale formula, expert-weights-norm GGUF default, RMSNorm/softmax/SiLU double-precision fidelity, a genuine Q2_K/Q8_0 kernel-order bug) but root-caused the remaining gap as this checkpoint's inherent routing-landscape flatness (~80% of top-6-of-64 MoE decisions sit at a margin under 0.005, identical from 2-bit to native 8-bit quantization) — not a discoverable code defect. **2026-09-02: formally accepted as final** (`docs/058-deepseek-full-lineage-implementation-plan.md` Phase 2) rather than left open pending further investigation; not admitted by the architecture gate, run explicitly via `--allow-unverified-arch`. A native, larger (non-Lite) V2/V3 checkpoint remains untried. Separately, confirmed 2026-09-11 via two real-weight `deepseek2`-tagged VLM checkpoints (Kimi-VL-A3B-thinking, YouTu-VL-4B): both genuinely have `q_lora_rank==0` (the "Lite" variant this codebase already supports for Q), but crash on `Missing tensor: blk.0.attn_kv_b.weight` — a narrower gap than full-size MLA: these checkpoints use the split `attn_k_b`/`attn_v_b` "absorption" K/V layout, which isn't read yet (only the legacy fused `attn_kv_b` tensor is), a real but more scoped fix than implementing `q_lora_rank>0` from scratch |
| DeepSeek-V3.2 (`deepseek32`) | 🔴 | 🔵 | — | Structurally complete alpha `IForwardPass` (`DeepSeek32ForwardPass.cs`): classic MLA attention with absorption, YaRN RoPE scaling (ported from the `deepseek2` investigation's verified formula chain), DSA/lightning-indexer sparse masking, dense/MoE FFN dispatch — but **never run once**, no DeepSeek-V3.2 GGUF available. Known gaps: Hadamard rotation on the indexer unimplemented, MTP unimplemented, MLA absorption math re-derived from the reference but not independently verified. See `docs/058-...md` Phase 1 |
| DeepSeek-V4 (`deepseek4`) | 🔴 | 🔵 | — | Structurally complete alpha `IForwardPass` (`DeepSeek4ForwardPass.cs`) covering all three of V4's attention variants (raw, HCA, CSA), hyper-connections, MoE with hash routing and sqrt-softplus gating — but **never run once**, no DeepSeek-V4-Flash GGUF available (smallest quant ~99 GB). Known gaps: Hadamard rotation unimplemented, MQA K==V and output-side `rope_ext_back` unverified, CSA's overlap-gather window is a working hypothesis not confirmed against the reference's actual index construction. See `docs/058-...md` Phase 0 |
| **Vision encoder rows below** | — | — | — | 17+ architectures implemented; broken out individually below since "one lumped line" undersells how differently these are each actually doing. `MultimodalRealWeightsTests.cs` real-weight-tests 11 with a differentiation/sanity check (real, non-degenerate embeddings; two genuinely different images must NOT produce near-identical output) on top of the golden numeric checks cited per-row below |
| Vision: Gemma4UV | 🟢 | 🔬 | CPU / CUDA / Vulkan | Golden-verified (cosine > 0.9995, `scripts/gemma4uv_ref.py`) against a hand-written numpy port of the real llama.cpp mtmd reference |
| Vision: Llava | 🟢 | 🔬 | CPU / CUDA / Vulkan | Golden-verified (cosine > 0.999, `scripts/llava_ref.py`) — 2 real bugs found and fixed along the way |
| Vision: Pixtral | 🟢 | 🔬 | CPU / CUDA / Vulkan | Golden-verified (cosine > 0.97, `scripts/pixtral_ref.py`) — 3 real bugs found and fixed |
| Vision: GLM-4.6V | 🟢 | 🔬 | CPU / CUDA / Vulkan | Golden-verified (cosine > 0.97, `scripts/glm4v_ref.py`) — 6 real bugs found and fixed (dead fused-QKV read, non-softmaxing attention stub, missing dual-patch-embed sum, dead patch-merger, wrong/missing projector-tail tensor names, a 4-section M-RoPE only rotating half of `head_dim` on the wrong axis) |
| Vision: Exaone4 | 🟢 | 🔬 | CPU / CUDA / Vulkan | Golden-verified (cosine > 0.97, `scripts/exaone4_ref.py`, 2026-09-02) — real windowed/local attention (`n_wa_pattern`-gated) |
| Vision: Qwen2.5-VL / Qwen3-VL | 🟢 | 🔬 | CPU / CUDA / Vulkan | Golden-verified (cosine > 0.97, `scripts/qwen25vl_ref.py`, 2026-09-02) — same M-RoPE bug class found again, plus a missing dual-patch-embed sum in `QwenVlVisionEncoder` |
| Vision: MiniCPM-V 2.6 | 🟢 | 🔬 | CPU / CUDA / Vulkan | **Fixed 2026-09-02**: resampler cross-attention was a dead stub that never read image-derived K/V at all — output was a fixed function of the learned query alone, identical for every image by construction. Now golden-verified |
| Vision: HunyuanVL (HunyuanOCR) | 🟢 | 🔬 | CPU / CUDA / Vulkan | **Fixed and golden-verified 2026-09-02** (cosine > 0.97, `scripts/hunyuanvl_ref.py`): real projector is a 3-stage strided-Conv2D + 1×1-Conv2D + `mm.model.fc` pipeline the encoder didn't implement at all — wrong tensor names, pixel-shuffle instead of real strided conv2d, raw truncated pos-embed instead of real bilinear resize, missing `image_newline`/`image_begin`/`image_end` wrapping. All four gaps fixed |
| Vision: InternVL 2.5 / 3 / 4 | 🟢 | 🔬 | CPU / Vulkan | Real, working end-to-end generation confirmed 2026-09-11 via the `--image` CLI on a real local checkpoint (InternVL3-2B) — genuinely describes real image content correctly, best-of-3 timed |
| Vision: Gemma 3 (SigLIP) | 🟢 | 🔬 | CPU | Real, working end-to-end generation confirmed 2026-09-11 via the `--image` CLI (Gemma-3-4B-it) — second confirmed-working real checkpoint alongside InternVL3-2B |
| Vision: MiMo-VL | 🟡 | 🔬 | CPU / CUDA / Vulkan | Encoder math golden-verified (cosine > 0.97, `scripts/mimovl_ref.py`, real windowed attention + M-RoPE + LayerNorm/GQA fixes) — but the actual local checkpoint (`mimo-vl-7b-sft-Q8_0.gguf`) has a real, upstream GGUF-conversion dimension mismatch (mmproj outputs 3584-dim, text backbone expects 4096-dim), confirmed 2026-09-11 and now surfaced as a clean error instead of a crash. Not usable end-to-end on this specific checkpoint until a correctly-converted mmproj exists |
| Vision: Kimi-VL | 🟡 | 🔬 | CPU / CUDA / Vulkan | Encoder math golden-verified (cosine > 0.97, `scripts/kimivl_ref.py`) — projector output-width and input-norm-dimension bugs fixed 2026-09-02. But the local checkpoint's text backbone (`deepseek2`) uses the split `attn_k_b`/`attn_v_b` MLA "absorption" K/V layout, which isn't read yet (only the legacy fused `attn_kv_b` tensor is) — `Missing tensor: blk.0.attn_kv_b.weight`, corrected 2026-09-11 (this is `q_lora_rank==0`, the already-supported "Lite" variant — an earlier note here wrongly attributed this to full-size MLA) |
| Vision: YoutuVL | 🟡 | 🔬 | CPU / CUDA / Vulkan | Encoder math golden-verified (cosine > 0.97, `scripts/youtuvl_ref.py`) — real projector tensor is `mm.2.weight` not `mm.1.weight`, fixed 2026-09-02. Same `deepseek2` full-MLA text-backbone gap as Kimi-VL above blocks end-to-end use on the local checkpoint |
| Vision: Step3-VL | 🟡 | 👂 | CPU | **2026-09-11**: found and fixed a real bug (wrong GGUF tensor name for the final projector, `mm.model_proj.weight` vs the real `mm.model.fc.weight`, confirmed against the real llama.cpp reference) — no longer crashes, runs to completion. Output is garbled (expected — Q2_K quant on an architecture without a validated forward-pass profile, run via `--allow-unverified-arch`). Not yet added to the golden-parity suite |
| Vision: dots.ocr | 🟡 | 👂 | CPU | Marker fix landed 2026-08-20 (real prefill/decode). **2026-09-11**: vision encoder runs correctly but decode emits a 1-token degenerate stop immediately — likely a prompt-format mismatch for this OCR-specialized checkpoint rather than an encoder bug, not root-caused |
| Vision: DeepSeek-OCR / DeepSeek-OCR2 | 🟡 | 👂 | CPU | **2026-09-11**: runs to completion via `--allow-unverified-arch` (`deepseek2-ocr`), real timing, but fully garbled mixed-language output — expected per the flag's own warning, architecture has no validated forward-pass profile |
| Vision: PaddleOCR-VL | 🟡 | 👂 | CPU | **2026-09-11**: runs via `--allow-unverified-arch` (`paddleocr`), real timing, but degenerate output (4 repeated newline-byte tokens) — architecture has no validated forward-pass profile |
| Vision: IBM Granite 4.0 Vision | 🔴 | 🔬 | CPU / Vulkan | **Two real bugs found and fixed 2026-09-11, still not fully correct.** The real multi-block windowed QFormer projector was ported (replacing a wrong single-linear-layer stand-in that fed raw untrained SigLIP hidden states as "image tokens"), then a second real bug found: hardcoded OpenAI CLIP normalization constants used instead of this checkpoint's real SigLIP `[0.5,0.5,0.5]` mean/std. Output behavior genuinely changed (confident, specific scene descriptions instead of generic non-image text) but is still not correctly grounded to real image content. See `docs/00-current-work.md`'s 2026-09-11 entries |
| Vision: IBM Granite Vision 3.2 | 🔴 | 👂 | CPU | Same class of degenerate, non-image-grounded output as Granite 4.0 Vision above, but this checkpoint's mmproj routes through a DIFFERENT code path (`LlavaAdapter`, `clip.projector_type: mlp`) — not yet confirmed whether it shares the Granite 4.0 root cause or is a separate LLaVA-path bug |
| Vision: NVIDIA Nemotron-Nano-12B-v2-VL | 🔴 | ⚪ | — | Vision encoder migrated and builds clean (register-token ViT + 2x2 patch merge), but the text backbone (`nemotron_h`, a Mamba/SSM hybrid) crashes before vision code ever runs (`HybridGdnForwardPass dense FFN requires hp.IntermediateDim > 0`). **Root-caused 2026-09-11**: the real metadata key exists as a per-layer array (0 = pure Mamba/SSM layer), not the scalar this codebase assumes — a real, moderate feature addition, not a quick fix, but smaller than "architecture not supported at all" |
| Vision: Llama 4 (vision path) | 🔴 | ⚪ | — | Text checkpoint explicitly not downloaded (93GB across 2 shards vs. this machine's 64GB RAM) — mmproj-only download not attempted; genuinely untested on this machine |
| Vision: MobileNetV5 | 🔴 | ⚪ | — | Encoder migrated, builds clean — but no real checkpoint on Hugging Face actually declares the `mobilenetv5`/`mobilenet_v5` projector type this adapter targets; the obvious candidate (gemma-3n) routes to `Gemma3Adapter` instead. Blocked on checkpoint availability, not a code gap |

Runs GGUF and SafeTensors models on CPU (AVX2/AVX-512 SIMD) and GPU (Vulkan compute shaders or CUDA cuBLAS), with:
- **OpenAI- and Anthropic-compatible API server** (/v1/chat/completions, /v1/audio/speech, /v1/audio/transcriptions), native dynamic Multi-LoRA serving,
- **Native Multimodal Vision Understanding** across 17+ architectures:
  - Alibaba Qwen2.5-VL / Qwen3-VL
  - DeepSeek-OCR & DeepSeek-OCR2
  - Mistral Pixtral 12B
  - LLaVA-1.5, LLaVA-NeXT, LLaVA-OneVision
  - OpenGVLab InternVL 2.5 / 3 / 4
  - OpenBMB MiniCPM-V 2.6
  - Zhipu AI GLM-4V, GLM-4.5V, GLM-OCR
  - NVIDIA Nemotron-V2-VL / Nemotron-4-Nano
  - Baidu / Dots PaddleOCR-VL & Dots-OCR
  - Moonshot AI Kimi K2.5 / Kimi-VL
  - LG Exaone 4.5-VL
  - IBM Granite Vision 3.2 / Granite 4.0 Vision (🔴 both — see the status table above for the real,
    root-caused reason)
  - Tencent Hunyuan-VL & Tencent Youtu-VL
  - Xiaomi MiMo-VL
  - StepFun Step3-VL
  - Google Gemma 4 unified (`gemma4uv`), Gemma 4 ViT (`gemma4v`), Gemma 3 SigLIP (`gemma3`), and Meta Llama 4 (`llama4`),
- **Native Speech-to-Text (ASR) & Forced Alignment**: OpenAI Whisper Large-v3 & Turbo with 100 languages, NVIDIA NeMo Parakeet FastConformer CTC (TDT decode head not implemented — confirmed 2026-09-11, only `ParakeetCtcDecoder.cs` exists), NVIDIA NeMo Citrinet CTC, Alibaba Qwen3-ASR 0.6B/1.7B, Qwen3-ForcedAligner, Silero VAD, NVIDIA NeMo MarbleNet VAD, and (as of 2026-09-11, real ONNX-backed pipelines, see the status table below) SenseVoice and FunASR Paraformer. The native GGUF Paraformer path is still broken (missing `pf.vocab` metadata, a bad conversion, not a code bug) — Paraformer now works via a separate, real ONNX path instead,
- **Native Text-to-Speech (TTS) Synthesis & Voice Cloning**: Alibaba Qwen3-TTS 12Hz (with ERes2NetV2 192-dim voice cloning speaker encoder), Coqui XTTS-v2 (GPT2 autoregressive codec + FiLM-conditioned HiFi-GAN, zero-shot voice cloning), Fish Speech S2 Pro, Kokoro-82M, Piper VITS, Meta MMS-TTS (Massively Multilingual VITS), F5-TTS Flow-Matching DiT, CosyVoice 2.0 / 300M, Chatterbox-Turbo, Orpheus-TTS (SNAC codec), Parler-TTS Mini, and MeloTTS Multilingual VITS with clause-level real-time streaming,
- **Native Studio-Grade DSP**: Rational windowed-sinc resamplers, ATSC A/85 broadcast downmixing, and TPDF noise-shaped dithered WAV export,
- **Native Diffusion & Video Synthesis Pipelines**: Stable Diffusion 1.5 (with ControlNet conditioning), SDXL, SD 3/3.5, FLUX.1, Stable Audio 3 Continuous MMDiT (Variable-Length 1s-6min 44.1kHz Stereo), MusicGen (T5-conditioned delayed-pattern autoregressive Transformer + EnCodec 32kHz), AudioGen (same architecture family, T5-large + 16kHz environmental-sound EnCodec), Z-Image-Turbo, Wan 2.1/2.2 Video, HunyuanVideo, and LTX-Video with zero-dependency Animated GIF and sequence exporters. **FLUX.2, FLUX 3, and Qwen Image & Edit have real code** (`src/OpenTail.Stingray.Diffusion/Flux2`/`Flux3`/`QwenImage`, each with its own DiT/RoPE/pipeline classes) **but have never been run against a real checkpoint** — confirmed 2026-09-11: their conformance tests (`Flux2ConformanceTests`/`Flux3ConformanceTests`/`QwenImageTests`) pass, but those are structural/synthetic-weight tests (RoPE orthogonality, shape checks), not real-weight generation — same "structurally complete, never run once" status as this README's DeepSeek-V3.2/V4 rows. Correcting an earlier note in this same edit pass that wrongly said these had zero implementation at all — they're real, just unverified against real weights.

---

## TTS engine benchmarks

Same test sentence ("Hello, I will make some lunch, darling!"), same reference speaker where voice
cloning applies, CPU-only (AVX2), full (non-streaming) generation. RTF = wall-clock seconds per
second of generated audio (lower is faster; 1.0× = real-time). TTFA = time-to-first-audio in
streaming mode, where supported.

| Rank | Engine | Family / Architecture | Sample Rate | Batch Latency (Audio Sec) | Batch RTF | Streaming TTFA | Sample |
|---|---|---|---|---|---|---|---|
| 🥇 | Piper | VITS (lessac-medium) | 22,050 Hz | 0.61s (3.24s) | 0.188× 🚀 | 89ms ⚡ | [piper-perf-turn1.wav](docs/audio-samples/piper-perf-turn1.wav) |
| 🥈 | MMS-TTS | VITS (mms-tts-eng) | 16,000 Hz | 1.42s (3.07s) | 0.463× ⚡ | 188ms ⚡ | [mms-tts-perf-turn2.wav](docs/audio-samples/mms-tts-perf-turn2.wav) |
| 🥉 | Kokoro | StyleTTS2 (82m-q8_0) | 24,000 Hz | 2.73s (2.93s) | 0.933× ⚡ | 1.27s ⚡ | [kokoro-perf-turn2.wav](docs/audio-samples/kokoro-perf-turn2.wav) |
| 4 | MeloTTS | VITS MRF (zh_en) | 44,100 Hz | 3.65s (2.73s) | 1.337× | 671ms ⚡ | [melotts-perf-turn2.wav](docs/audio-samples/melotts-perf-turn2.wav) |
| 5 | XTTS-v2 | GPT-2 + HiFi-GAN | 24,000 Hz | 11.28s (3.85s) | 2.930× | 1.33s ⚡ | [xtts-perf-turn2.wav](docs/audio-samples/xtts-perf-turn2.wav) |
| 6 | QwenTTS | Qwen-Talker 0.6B + DAC | 24,000 Hz | 6.38s (2.16s) | 2.954× | 465ms ⚡ | [qwen-tts-perf-turn5.wav](docs/audio-samples/qwen-tts-perf-turn5.wav) |
| 7 | Chatterbox | Llama3-AR + S3Gen | 24,000 Hz | 11.55s (2.44s) | 4.733× | 11.47s (sentence) | [chatterbox-perf-turn1.wav](docs/audio-samples/chatterbox-perf-turn1.wav) |
| 8 | Parler-TTS | Transformer + DAC (mini-v1) | 44,100 Hz | 14.52s (2.57s) | 5.659× | 1.73s ⚡ | [parler-perf-turn2.wav](docs/audio-samples/parler-perf-turn2.wav) |
| 9 | CosyVoice 3 | DiT Flow Matching | 24,000 Hz | 22.13s (2.96s) | 7.476× | — | [cosyvoice3-perf-turn2.wav](docs/audio-samples/cosyvoice3-perf-turn2.wav) |
| 10 | F5-TTS | DiT Flow Matching (Q8_0 + decayed CFG) | 24,000 Hz | 35.96s (2.77s) | 12.965× | 35.8s (sentence) | [f5tts-perf-turn4.wav](docs/audio-samples/f5tts-perf-turn4.wav) |
| 11 | FishSpeech | Dual-AR + Firefly | 44,100 Hz | 50.67s (3.11s) | 16.285× | 1.76s ⚡ | [fishspeech-perf-turn3.wav](docs/audio-samples/fishspeech-perf-turn3.wav) |

Raw run data: [docs/tts-benchmark-log.txt](docs/tts-benchmark-log.txt).

---

## Try it

```bash
dotnet tool install -g OpenTail.Stingray.Cli

stingray -m models/SmolLM2-1.7B-Instruct-Q4_K_M.gguf -p "Once upon a time"
stingray -m models/Qwen3-8B-Q4_K_M.gguf -p "Explain mmap" -g -1     # all layers on GPU
stingray -m models/Qwen3-8B-Q4_K_M.gguf                             # interactive chat
stingray tts -t "Hello from OpenTail Stingray!" -v af_heart -o speech.wav # native TTS
```

Or use it as a library:

```bash
dotnet add package OpenTail.Stingray --version 1.0.6
```

| Package | What it gives you |
|---|---|
| [`OpenTail.Stingray`](https://www.nuget.org/packages/OpenTail.Stingray) | The engine — all core, vision, audio, diffusion, and hardware assemblies in one package |
| [`OpenTail.Stingray.Cli`](https://www.nuget.org/packages/OpenTail.Stingray.Cli) | The `stingray` command-line tool |
| [`OpenTail.Stingray.Server`](https://www.nuget.org/packages/OpenTail.Stingray.Server) | ASP.NET Core endpoints — OpenAI, Anthropic and Responses APIs |

**Coming from llama.cpp?** Flag names follow `llama-cli` where the meaning matches, single-dash
spellings included (`-ngl`, `-ctk`, `-md`, `-fa`). Flags that cannot be honoured are refused with a
named reason rather than silently ignored.

**Requirements:** .NET 10, x86-64 with AVX2. GPU is optional — any Vulkan-capable card, or NVIDIA
with CUDA 12.x. Building from source needs the .NET 10 SDK and `dotnet build -c Release`.

---

## License

MIT License — Copyright (c) 2026 OpenTail.
